{% include 'flash-notices.html.twig' %}

{{ form_errors(form) }}

{{ form_start(form, {'attr': {'class' : 'recovery-form'}}) }}

	{{ form_label(form.email) }}
	{{ form_widget(form.email) }}
	{{ form_errors(form.email) }}

	<input class="button" type="submit" value="{{ 'Отправить запрос'|trans }}">
{{ form_end(form) }}